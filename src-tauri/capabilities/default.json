{
  "identifier": "default",
  "description": "Default desktop capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom"
  ]
}
